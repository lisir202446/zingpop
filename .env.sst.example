# Shared deploy credentials
CLOUDFLARE_API_TOKEN=
PLANETSCALE_SERVICE_TOKEN_NAME=
PLANETSCALE_SERVICE_TOKEN=

# Either set STRIPE_SECRET_KEY directly in the stage-specific file,
# or keep both names here and let sst pick by stage.
STRIPE_SECRET_KEY_DEV=
STRIPE_SECRET_KEY_PROD=
